spring:
  application:
    name: ncms-backend

  # PostgreSQL Database Configuration
  datasource:
    url: 'jdbc:postgresql://sakura.proxy.rlwy.net:17451/railway'
    driver-class-name: 'org.postgresql.Driver'
    username: 'postgres'
    password: 'YuTJvljTRuIwrjnnqKyGlKeIWBGYVLAG'

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false

# Server Configuration
server:
  port: 8080

# CORS Configuration
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://ncms-production.up.railway.app,http://ncms-production.up.railway.app,https://ncms-omega.vercel.app}

# Logging
logging:
  level:
    kr.co.tobetheone.ncms: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

