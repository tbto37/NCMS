spring:
  application:
    name: ncms-backend

  # PostgreSQL Database Configuration
  datasource:
    url: 'jdbc:postgresql://sakura.proxy.rlwy.net:17451/railway'
    driver-class-name: 'org.postgresql.Driver'
    username: 'postgres'
    password: 'YuTJvljTRuIwrjnnqKyGlKeIWBGYVLAG'

  # Mail Configuration
  mail:
    host: ${SPRING_MAIL_HOST:smtp.naver.com}
    port: ${SPRING_MAIL_PORT:465}
    username: ${SPRING_MAIL_USERNAME:chohw37@naver.com}
    password: ${SPRING_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: smtp.naver.com

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false

# Server Configuration
server:
  port: 8080

# CORS Configuration
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://logcom.co.kr,http://logcom.co.kr,https://*.logcom.co.kr,http://*.logcom.co.kr,https://api.logcom.co.kr,http://api.logcom.co.kr,https://ncms-omega.vercel.app,https://*.vercel.app,http://localhost:5173,http://localhost:3000}

# Logging
logging:
  level:
    kr.co.tobetheone.ncms: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

