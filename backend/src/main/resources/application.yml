spring:
  application:
    name: ncms-backend

  # H2 Database Configuration (Development)
  datasource:
    url: jdbc:postgresql://sakura.proxy.rlwy.net:17451/railway
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: YuTJvljTRuIwrjnnqKyGlKeIWBGYVLAG

  h2:
    console:
      enabled: true
      path: /h2-console

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
        jdbc:
          time_zone: UTC

  # Flyway Configuration (disabled for H2 development)
  flyway:
    enabled: false

  # Security - Temporarily disable for initial setup
  security:
    user:
      name: admin
      password: admin123

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api

# Logging
logging:
  level:
    kr.co.tobetheone.ncms: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.flywaydb: DEBUG
